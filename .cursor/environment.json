{
  "name": "rs-sdk swarm",
  "install": "command -v bun >/dev/null || (curl -fsSL https://bun.sh/install | bash); export PATH=\"$HOME/.bun/bin:$PATH\"; bun install --frozen-lockfile; (cd server/webclient && bun install --frozen-lockfile)"
}
